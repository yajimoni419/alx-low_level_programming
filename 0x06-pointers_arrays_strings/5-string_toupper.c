#include "main.h"
#include <stdio.h>

/**
 * _string-toupper - change all letters from uppercase to lowercase
 * @n: pointer
 * Return: n
 */
char *string_toupper(char *);
{
	int i;

	int = 0;
	while (n[i] != '\0')
	{
		if (n[i] >= 'a' && n[i] <= 'i')
			n[i] = n[i] - 32;
		i++;
	}
	return (n);
}

